baseURL = ''
locale = 'en-us'
title = 'Training App Notes'

# MOVE THIS TO THE TOP LEVEL (OUT OF PARAMS)
enableGitInfo = true

# ADD THIS BLOCK
[frontmatter]
  lastmod = [':git', 'lastmod', 'date', 'publishDate']

[module]
  [[module.imports]]
    path = 'github.com/alex-shpak/hugo-book'

[params]
  BookSection = 'docs'
  BookTheme = 'auto'
  BookSearch = true
  BookFavicon = 'favicon-96x96.png'
  BookDateFormat = 'January 2, 2006'
  # This specific theme parameter relies on enableGitInfo being true at the top level
  BookLastChangeLink = 'https://github.com/jackrschumacher/Training-app-docs/commit/{{ .GitInfo.Hash }}'

[markup]
  [markup.tableOfContents]
      startLevel = 1
      endLevel = 6
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true